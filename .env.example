# Anthropic API key (for Ask GEO Pulse chat sidebar, optional)
ANTHROPIC_API_KEY=

# Slack export path (optional, defaults to data/slack_export.json)
SLACK_EXPORT_PATH=data/slack_export.json

# Scheduler interval in hours (default 6)
SCRAPE_INTERVAL_HOURS=6
